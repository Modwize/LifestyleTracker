{
  "name": "@modwize/lifestyletracker",
  "version": "0.1.0",
  "private": true,
  "description": "Adherence-first transformation coaching — schema + event logic layer.",
  "type": "module",
  "main": "src/engine/index.ts",
  "scripts": {
    "test": "node --test --experimental-strip-types src/engine/__tests__/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
